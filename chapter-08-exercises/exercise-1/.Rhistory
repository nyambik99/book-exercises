# Create a vector `my_breakfast` of everything you ate for breakfast
my_breakfast <- c("milk", "oatmeal", "bacon", "banana", "cigarettes")
# Create a vector `my_lunch` of everything you ate (or will eat) for lunch
my_lunch <- c("fried_rice", "ground beef", "vegetables", "cigarettes" )
# Create a list `meals` that has contains your breakfast and lunch
mealse <- list(my_breakfast, my_lunch)
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(my_breakfast, my_lunch)
# Create a vector `my_breakfast` of everything you ate for breakfast
my_breakfast <- c("milk", "oatmeal", "bacon", "banana", "cigarettes")
# Create a vector `my_lunch` of everything you ate (or will eat) for lunch
my_lunch <- c("fried_rice", "ground beef", "vegetables", "cigarettes" )
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(my_breakfast, my_lunch)
# Create a list `meals` that has contains your breakfast and lunch
meals <- list("breakfast" = my_breakfast, "lunch" = my_lunch)
# Add a "dinner" element to your `meals` list that has what you plan to eat
# for dinner
meals[["dinner"]] <- c("pizza", "sprite", "milk")
meals$dinner
# Add a "dinner" element to your `meals` list that has what you plan to eat
# for dinner
meals[["dinner"]] <- c("pizza","sprite", "milk")
meals$dinner
# Use dollar notation to extract your `dinner` element from your list
# and save it in a vector called 'dinner'
dinner <- c(meals[["dinner"]])
# Add a "dinner" element to your `meals` list that has what you plan to eat
# for dinner
meals[["dinner"]] <- c("pizza","sprite", "milk", "cigarettes")
# Use dollar notation to extract your `dinner` element from your list
# and save it in a vector called 'dinner'
dinner <- c(meals[["dinner"]])
# Use dollar notation to extract your `dinner` element from your list
# and save it in a vector called 'dinner'
dinner <- c(meals$dinner)
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[5] <- meals[["lunch"]]
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[5]] <- meals[["lunch"]]
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- meals[["breakfast", "lunch"]]
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- meals["breakfast", "lunch"]
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- list(meals["breakfast", "lunch"])
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- meals[c("breakfast", "lunch")]
?lappy
?lapply
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
lapply(length(meals))
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
lapply(meals, length(meals))
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
lapply(meals, length())
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
lapply(meals, length)
return(meals)
return(meals)
add_pizza <- function() {
meals <- "pizza"
return(meals)
}
View(add_pizza)
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- add_pizza()
add_pizza <- function() {
meals[] <- "pizza"
return(meals)
}
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- add_pizza()
meals <- meals[meals, "pizza"]
add_pizza <- function() {
meals <- meals[meals, "pizza"]
return(meals)
}
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- add_pizza()
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- add_pizza()
add_pizza <- function() {
meals <- list(meals, "pizza")
return(meals)
}
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- add_pizza()
?seq
meals[seq(from = 1, to = 5)]
meals <- append(meals, "pizza")
meals <- append(meals)
meals <- append(meals, list("pizza"))
meals[6, 7]
meals[6]
meals[- 6]
meals[- 6]
meals <- meals[- 7]
meals <- meals[- 6]
meals <- meals[- 4]
meals <- meals[- 4]
add_pizza <- function(list) {
for(i in 1:length(list)) {
list[i] <- paste(list[[i]], "pizza")
}
return(list)
}
# Create a vector `better_meals` that is all your meals, but with pizza!
add_pizza(meals)
add_pizza <- function(list) {
for(i in 1:length(list)) {
list[[i]] <- paste(list[[i]], "pizza")
}
return(list)
}
# Create a vector `better_meals` that is all your meals, but with pizza!
add_pizza(meals)
add_pizza <- function(list) {
for(i in 1:length(list)) {
list[[i]] <- append(list[[i]], "pizza")
}
return(list)
}
# Create a vector `better_meals` that is all your meals, but with pizza!
add_pizza(meals)
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- add_pizza(meals)
